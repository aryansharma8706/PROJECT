import { Blocker, BlockerFunction, Location } from 'react-router';
declare function usePrompt(when: boolean | BlockerFunction, onNavigate?: (nextLocation: Location) => void): Blocker;
export default usePrompt;
